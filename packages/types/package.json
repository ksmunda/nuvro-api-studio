{
  "name": "@nuvro/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types derived from Zod validation schemas",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@nuvro/validation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@nuvro/config": "workspace:*"
  }
}
